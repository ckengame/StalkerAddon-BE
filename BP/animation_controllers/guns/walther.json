{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.player.walther": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('walther') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:walther') && q.is_using_item" },
						{ "shoot": "q.scoreboard('walther') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:walther') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s walther.reload"],
					"animations": ["walther_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:walther') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["walther_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}