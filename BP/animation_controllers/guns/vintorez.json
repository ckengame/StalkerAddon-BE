{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.player.vintorez": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('vintorez') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:vintorez') && q.is_using_item" },
						{ "shoot": "q.scoreboard('vintorez') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:vintorez') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s vintorez.reload"],
					"animations": ["vintorez_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:vintorez') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["vintorez_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}