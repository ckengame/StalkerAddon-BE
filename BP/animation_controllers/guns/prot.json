{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.player.prot": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('prot') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:prot') && q.is_using_item" },
						{ "shoot": "q.scoreboard('prot') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:prot') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s prot.reload"],
					"animations": ["prot_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:prot') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["prot_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}