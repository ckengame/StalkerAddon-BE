{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.player.groza": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('groza') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:groza') && q.is_using_item" },
						{ "shoot": "q.scoreboard('groza') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:groza') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s groza.reload"],
					"animations": ["groza_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:groza') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["groza_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}