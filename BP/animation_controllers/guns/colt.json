{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.player.colt": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('colt') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:colt') && q.is_using_item" },
						{ "shoot": "q.scoreboard('colt') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:colt') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s colt.reload"],
					"animations": ["colt_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:colt') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["colt_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}