{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.player.hpsa": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('hpsa') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:hpsa') && q.is_using_item" },
						{ "shoot": "q.scoreboard('hpsa') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:hpsa') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s hpsa.reload"],
					"animations": ["hpsa_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:hpsa') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["hpsa_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}