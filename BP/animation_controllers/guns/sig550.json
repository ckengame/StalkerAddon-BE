{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.player.sig550": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('sig550') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:sig550') && q.is_using_item" },
						{ "shoot": "q.scoreboard('sig550') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:sig550') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s sig550.reload"],
					"animations": ["sig550_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:sig550') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["sig550_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}