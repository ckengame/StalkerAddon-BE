{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.winch": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('winch') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:winch') && q.is_using_item" },
						{ "shoot": "q.scoreboard('winch') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:winch') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s winch.reload"],
					"animations": ["winch_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:winch') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["winch_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}