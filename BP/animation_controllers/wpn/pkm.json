{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.player.pkm": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('pkm') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:pkm') && q.is_using_item" },
						{ "shoot": "q.scoreboard('pkm') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:pkm') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s pkm.reload"],
					"animations": ["pkm_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:pkm') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["pkm_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}