{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.fort": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('fort') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:fort') && q.is_using_item" },
						{ "shoot": "q.scoreboard('fort') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:fort') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s fort.reload"],
					"animations": ["fort_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:fort') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["fort_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}