{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.pm": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('pm') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:pm') && q.is_using_item" },
						{ "shoot": "q.scoreboard('pm') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:pm') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s pm.reload"],
					"animations": ["pm_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:pm') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["pm_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}