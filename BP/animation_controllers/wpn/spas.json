{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.spas": {
			"initial_state": "default",
			"states": {
				"default": {
					"transitions": [
						{ "reload": "q.scoreboard('spas') == 0 && q.is_item_name_any('slot.weapon.mainhand', 'scm:spas') && q.is_using_item" },
						{ "shoot": "q.scoreboard('spas') >= 1 && q.is_item_name_any('slot.weapon.mainhand', 'scm:spas') && q.is_using_item" }
					]
				},
				"reload": {
					"on_exit": ["@s gun:base", "/stopsound @s spas.reload"],
					"animations": ["spas_reload"],
					"transitions": [
						{ "default": "q.all_animations_finished || !q.is_item_name_any('slot.weapon.mainhand', 'scm:spas') || q.mark_variant != 2" }
					]
				},
				"shoot": {
					"on_entry": ["@s gun:shoot"],
					"animations": ["spas_shoot"],
					"transitions": [
						{ "default": "q.all_animations_finished" }
					]
				}
			}
		}
	}
}